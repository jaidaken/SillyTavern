diff --git i/src/runtime/core/vdom.zig w/src/runtime/core/vdom.zig
index 6f32cd03..cc0367e9 100644
--- i/src/runtime/core/vdom.zig
+++ w/src/runtime/core/vdom.zig
@@ -313,8 +313,9 @@ pub fn diff(
                                 },
                             },
                         });
-                        old_vnode.component = .{ .text = new_text };
                     }
+                    // Always adopt the current render's pointer: the previous one may be freed.
+                    old_vnode.component = .{ .text = new_text };
                 },
                 else => {},
             }
