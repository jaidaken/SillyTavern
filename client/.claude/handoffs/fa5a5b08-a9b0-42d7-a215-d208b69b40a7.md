{"ts":1783736753,"s":"fa5a5b08-a9b0-42d7-a215-d208b69b40a7","cwd":"/home/jaidaken/projects/SillyTavern","g":{"head":"d67739012","branch":"jaidaken","dirty":"check"},"team":"sillytavern-client-perf","status":"LAYING TO BED mid-fix; edge rebuild IN FLIGHT","roster":[{"n":"m-mainjs","state":"idle-DONE-needs-teardown","wt":"/home/jaidaken/projects/ST-wt-mainjs","task":"diagnosed the main.js minify break; READ ITS REPORT before teardown - it likely confirms m-tt's finding that the real culprit was the FONT PRELOAD (old-headless virtual-time starvation), NOT the minify. m-perf + m-tt already shut down."}],"WHAT_HAPPENED":"TrustedTypes DOM-XSS hardening. Bug chain: (1) perf commit 6b73a3b57 msg claimed 'Trusted Types sinks + font preload' but only build.sh (lib+css minify) actually landed - m-tt's main.js/layout.zx edits were lost in a worktree shuffle. (2) edge CSP require-trusted-types-for 'script' was committed (nixos 6cd774d) + rebuilt while the client had NO TT policy -> live sillybeta BROKE. (3) I first mis-blamed minifying main.js; m-tt's controlled test proved the real verify.sh regression was the FONT PRELOAD starving old-headless --virtual-time-budget (held-stream tests empty), NOT minify and NOT TT.","FIX_DONE":"client: 'default' passthrough TT policy at glue/main.js boot (browser auto-applies to EVERY innerHTML incl the ziex door - simpler + more complete than per-sink wrap). committed d67739012 + DEPLOYED to bigboy (verify.sh rc=0). VERIFIED under enforcing CSP locally via Playwright: 12/12 bodies render, console clean with 'trusted-types default dompurify'. edge: CSP changed 'trusted-types st-html' -> 'trusted-types default dompurify' (DOMPurify needs its own 'dompurify' policy), committed in ~/nixos-config, edge REBUILD IN FLIGHT (log /tmp/claude-1000/-home-jaidaken-projects-SillyTavern/fa5a5b08-a9b0-42d7-a215-d208b69b40a7/scratchpad/edge-rebuild3.log, watcher armed).","RESUME_NEXT":"1 CONFIRM edge rebuild finished: grep REBUILD_EXIT= in edge-rebuild3.log (expect 0). 2 VERIFY live unbroken: curl -sI https://sillybeta.jaidaken.dev shows CSP 'trusted-types default dompurify', Caddy active on edge, other edge sites 200. app-under-CSP = operator loads authed sillybeta + checks console clean (or local Playwright: build client, sed the CSP meta into dist/index.html, serve, check 12 bodies + 0 console errors). 3 read m-mainjs report, teardown m-mainjs + git worktree remove ST-wt-mainjs. 4 DEFERRED PERF: re-enable main.js minify in build.sh (m-perf proved it safe/green; my exclusion was the wrong diagnosis) AND resolve the font preload (real perf win but breaks verify.sh old-headless; either switch verify chrome() to --headless=new, or drop the crossorigin preload - m-tt flagged the tradeoff). 5 rollback if needed: sillybeta.old on bigboy; edge = git revert + rebuild edge.","done_earlier":"wave1 a8ef09a18, gate 56e178e74, app-correctness 5102f5b03, css f09bd400a, code-fence ee219af81, leaks 6d91a78f4, scriptorium a4247e29b, rel-fix 4a2adbdff, placeholder 30244f579, staggered-reveal+12msg 537e6a54c, seo 0b88581c9, perf-lib-minify 6b73a3b57, edge-security-headers 2b1a3f6, edge-TT-CSP 6cd774d. all deployed. deploy: client=tar|ssh proxmox atomic-swap to /media/bigboy/sillybeta; edge='rebuild edge' from ~/nixos-config."}
